// AI-8594: stub restored so matches/page.tsx type-checks. The full offline
// contact form UI was lost during the feature -> main merge; until it's
// rebuilt, this render-no-op keeps the page buildable.
export default function OfflineContactForm() {
  return null
}
